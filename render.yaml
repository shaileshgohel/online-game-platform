services:
  - type: web
    name: pulsequiz-live
    runtime: node
    plan: free
    buildCommand: npm install && npm run build:all
    startCommand: npm start
    healthCheckPath: /healthz
    envVars:
      - key: NODE_VERSION
        value: 22